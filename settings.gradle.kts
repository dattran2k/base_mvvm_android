pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven("https://maven.google.com")
    }
}
rootProject.name = "Base MVVM Single Module"
include(":app")